name: 'Path Traversal (URI)'
type: 'exploit'

text: |-
  Time to bypass the rule(s) below! You're on your own now, but feel free to
  ask questions!

  **Goal: Print (`cat`) `/app/server.rb`**

hints: []

target:
  dev:
    host: localhost
    port: 10002
  prod:
    host: detector
    port: 80

base_request: |-
  GET /../../../../../../../app/server.rb HTTP/1.1
  Host: localhost

rules:
- >
  alert http any any -> any any (msg:"Gotcha!"; flow:to_server,established; http.uri.raw; content: "../"; sid:123456; rev:1;)

expected_output: "TCPServer.new"

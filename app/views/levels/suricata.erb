<%= erb :'partials/header', :locals => { title: level['name'] } %>
<body>
  <div class="container-fluid">
    <%= erb :'partials/sidebar', :locals => { levels: levels } %>

    <div class="main-content">
      <%= erb :'partials/level-header', :locals => { level: level } %>

      <%= erb :'components/text', :locals => { level: level } %>

      <div class="row">
        <div class="col-md-6">
          <%= erb :'components/suricata-rule', :locals => { level: level } %>

          <%= erb :'components/results', :locals => { level: level } %>
        </div>

        <div class="col-md-6">
          <%= erb :'components/suricata-requests', :locals => { level: level } %>
        </div>
      </div>

    </div>
  </div>
  <%= erb :'partials/scripts' %>
  <script src="/suricata.js"></script>
</body>
</html>
